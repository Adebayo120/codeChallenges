<?php

function TSPDynamicApproach(int $startPoint, array $graph, array $subsets)
{
    return TSPDynamicApproachAction()
    
}

function TSPDynamicApproachAction(int $startPoint, array $graph, array $subsets)
{
    if (empty($subsets)) {
        return $graph[]
    }
}